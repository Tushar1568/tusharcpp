#include<bits/stdc++.h>
using namespace std;
int main(){
    int n,m;
    cin>>n>>m;
    int arr[n];
    for (int i = 0; i < n; i++)
    {
        cin>>arr[i];
    }
    arr[0]=x;
    int sum;
    for (int j= 1; j<m; j++)
    {
        sum+=arr[j];
    }
    for (int k = n-1; k>=0; k--)
    {
       max+=arr[k];
       if(k==n-m=1)
       break;
    }
    if(sum>=max){
        cou<<max
    }
    
    
    
    
}